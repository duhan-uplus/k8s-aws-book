server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # 중요: 요청 파일이 없으면 index.html을 반환 (SPA 라우팅)
        try_files $uri $uri/ /index.html;
    }
}

